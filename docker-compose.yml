version: '3.8'

services:
  phishing_api:
    build: .
    container_name: phishing_v1_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./models:/app/models
    restart: unless-stopped
